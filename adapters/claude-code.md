# Adapter: Claude Code

Claude Code can be used as a repository-aware coding assistant when connected to local tools.

It must follow the same rules, reviews, and approval gates as every other adapter.
