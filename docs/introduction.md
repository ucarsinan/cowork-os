# Introduction

CoWork-OS provides a structured way to coordinate AI-assisted work.

The framework turns ad hoc prompting into a repeatable loop:

```text
Intake -> Audit -> Plan -> Approval -> Work -> Review -> Verification -> Handoff
```

The files are plain Markdown so they can be adapted to different tools, repositories, and team practices.
