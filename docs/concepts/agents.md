# Agents

Agents are reusable responsibility profiles.

An agent file should define:

- mission
- input
- output
- boundaries
- stop rules

Agents can be combined in one assistant session, but the output should make the active role clear.
